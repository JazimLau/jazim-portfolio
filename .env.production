# ─────────────────────────────────────────────────────────────
# 生产环境变量（所有 Production Build 共用，mode=production 时自动加载）
# 注意：这里只有「公开前端 URL / CDN 域名」，不是 Secret。
# 绝对禁止把 SecretId / SecretKey / Token 放进任何 .env* 文件。
# ─────────────────────────────────────────────────────────────

# 大型 HLS 媒体统一指向 Tencent COS 默认域名（免备案，无需 DNS/自定义域名）
# 说明：大陆地域 COS 绑定自定义域名需要 ICP 备案；当前无备案，使用 COS 默认域名。
# 备案通过后可切回自定义域名 media.jazimportfolio.com（届时仅需改此处并重建）。
# 本地开发（npm run dev）不加载此文件，继续使用本地 public/assets/videos/。
VITE_MEDIA_BASE_URL=https://jazimprofile-media-1465643833.cos.ap-guangzhou.myqcloud.com
